# TODO
- [ ] Update admin user creation for tests by setting `is_admin` (or adjusting middleware condition) so admin routes return 200/redirect instead of 403.
- [ ] Re-run `php artisan test` to confirm all tests pass.

